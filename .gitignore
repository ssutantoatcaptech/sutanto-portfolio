# Failed Claude Design exports: five files that each re-exported the landing
# page instead of their named page. Kept locally until the pages are re-exported.
*Standalone*.html

# Superseded working copy of the deck. The deck itself now lives in deck/;
# what remains here is the original working folder plus two duplicate copies
# of the deck PDF that no page references.
uploads/Deck modification/
uploads/steve-sutanto-portfolio-1.1/

# Zipped re-export of this same repo. Everything in it is already tracked.
*.zip

# macOS folder metadata
.DS_Store
